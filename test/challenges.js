let sampleOrder = "cba";
let sampleStr = "abcd";

let sampleArr = [0, 2, 11, 7];

function twoSum(nums, target) {
    let targetArr = [];
    let sum = 0;
    let count = 1;
    while (count < nums.length) {
        sum = 
    }
}

twoSums(sampleArr, 9); add .add